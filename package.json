{
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && python manage.py collectstatic --noinput",
    "start": "gunicorn gloomwoodmanor.wsgi"
  },
  "dependencies": {
    "phaser": "^3.88.2",
    "react-router-dom": "^7.5.0"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
